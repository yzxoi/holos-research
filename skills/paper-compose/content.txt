# Paper Compose — From Claims to Complete Manuscript

> **User request:** $ARGUMENTS

## ⚠️ CRITICAL RULES

1. **ALL research files under `.research/`** — paper source files go in `paper/` directory.
2. **Exhibits via `research_exhibit(action="create")`** — writes to `.research/exhibits/`.
3. **Paper via `research_paper(action="create")`** — writes to `.research/manuscripts/`.
4. **Figure scripts** → `paper/scripts/` or `.research/scripts/`. Generated figures → `paper/figures/`.
5. **Subagents (scribe/editor) return text, not files.** The main agent writes to the correct location.
6. **Follow AGENTS.md at project root** — code discipline, directory structure, tool usage, naming conventions.
7. **End session with `research_state(action="brief")`** to update AGENTS.md dynamic state.
8. **Git commit research artifacts** after modifying .research/ files.

---

## Skill Checklist (v2.2)

### Must
- [ ] Every paper-visible result references specific experiment/exhibit
- Record decision rationale in journal via `research_journal(action="append", kind="decision_rationale", note="...")`
- [ ] Claims map to RQG-backed evidence
- [ ] StorySpine drives intro arc (field_assumption → pain_point → gap → insight → method → evidence → implication)
- [ ] Overclaim check passes before paper-ready
- [ ] Pre-writing gates pass: contribution_check.py + results_validation_check.py exit 0 before drafting
- [ ] Record used_wiki_refs in phase_run.context_refresh

### Forbidden
- [ ] Do not state strong claims without RQG pass
- [ ] Do not ignore negative/weak results
- [ ] Do not fabricate or approximate metrics

### Checkpoints
- [ ] paper_ambition: User confirms claim strength (strong/moderate/weak)
- [ ] pre_write_gates: contribution + results-validation + reviewer-audit all pass before drafting
- [ ] submission_readiness: User confirms before submission

---

Load references as needed:
- `references/contribution.md` — 贡献契约四段式 + 检查清单(写作前硬门禁)
- `references/results-validation.md` — Results 七列验证矩阵(防 metric dump)
- `references/reviewer-audit.md` — 审稿人 objection 登记(写作前预演)
- `references/scenario-journal.md` — 期刊场景 playbook(目标 venue 为期刊时)
- `references/scenario-conference.md` — 会议场景 playbook(目标 venue 为会议时)
- `references/exhibit-guide.md` — figure/table decision routing and quality standards
- `references/data-figures.md` — 17 chart types with code templates
- `references/diagram-guide.md` — architecture diagrams (Mermaid/draw.io/TikZ)
- `references/writing-guide.md` — narrative strategies, scene routing, contribution contract
- `references/prose-quality.md` — paragraph/sentence level quality (5-pass audit, reverse outline test)
- `references/latex-conventions.md` — compilation, citation guard, numeric consistency, venue LaTeX rules

---

## DAG Template

Create this DAG at skill entry. Exhibit production and structure can start in parallel.

```
dagwrite({ nodes: [
  { id: "load",        content: "Load claims, experiments, wiki context",            status: "pending", deps: [] },
  { id: "exh-plan",    content: "Map claims to exhibits, plan all figures/tables",   status: "pending", deps: ["load"] },
  { id: "gates",       content: "Pre-writing gates: contribution, results-validation, reviewer-audit, blueprints", status: "pending", deps: ["load"] },
  { id: "exh-produce", content: "Generate, render, verify, approve exhibits",        status: "pending", deps: ["exh-plan"] },
  { id: "structure",   content: "Register paper, bind claims/exhibits, set sections", status: "pending", deps: ["exh-plan"] },
  { id: "quality",     content: "5-pass prose audit + reverse outline test",         status: "pending", deps: ["write"] },
  { id: "coherence",   content: "Full-paper coherence check via editor",             status: "pending", deps: ["quality"], assign: "editor" },
  { id: "write",       content: "Section-by-section writing via scribe+editor",      status: "pending", deps: ["exh-produce", "structure", "gates"] },
  { id: "supplement",  content: "Write supplementary material",                       status: "pending", deps: ["write"] },
  { id: "compile",     content: "LaTeX compilation and formatting check",            status: "pending", deps: ["coherence", "supplement"] },
  { id: "present",     content: "Present to user, advance to audit",                 status: "pending", deps: ["compile"] }
]})
```

`exh-produce`, `structure`, and `gates` run in parallel after loading context.
`quality` and `supplement` can also overlap if supplementary doesn't depend on main-text revisions.

---

## Prerequisites

- `research_state` shows `phase: compose`
- Core claims are `final` via `research_claim`
- Experiment results are complete and integrity-checked

## Output contract

- A `paper_XXX` object exists with status `ready`
- All main-text figures and tables are registered as `exhibit` objects with status `approved`
- Claims and exhibits are bound to the paper via `research_paper(bind)`
- LaTeX/markdown source exists in `paper/` directory
- Supplementary material structure is planned

---

## Context Refresh (Mandatory at Entry)

Before beginning any work in this phase:

1. **Load core state**:
   ```
   research_state(action="read")
   ```
   - Load `state.focus`, `active_phase_run`, and `anchor`
   - Identify active paper from `focus.refs.paper`

2. **Load relevant claims, experiments, and positioning**:
   - Read final claims via `research_claim(action="list")`
   - Read experiments via `research_experiment(action="list")`
   - Read `positioning/idea_*.story.yaml` for grounded StorySpine
   - Read `positioning/story_spine_*.yaml` for StorySpine (grounded_angle provides the paper path direction)
   - Read active plan's `.md` for method design
   - Read idea's `.md` for contribution statement and positioning matrix

3. **Load wiki context**:
   ```
   research_wiki(action="query")
   ```
   - Read relevant `papers/{slug}.md` for related work references
   - Read wiki edges for literature relationships

4. **Record wiki refs**:
   - Record all `used_wiki_refs` in `phase_run.context_refresh`

5. **Check skill checklist rules**:
   - Review Must/Forbidden/Checklists below
   - Record `checked_skill_rules` in `phase_run.context_refresh`

6. **Drift check**:
   - Verify paper composition still addresses `anchor` and grounded StorySpine
   - If drift detected, flag before proceeding

After refresh, write context recovery to the phase run:
```
research_state(action="brief", context_refresh={trigger: "session_resume", loaded: ["state","claims","experiments","plan","wiki"], checked_skill_rules: true})
```

---

## Step 0: Load Context

```
research_state(action="read")
research_claim(action="list")         // all final claims
research_experiment(action="list")    // all experiments for result data
research_wiki(action="query")         // literature landscape for related work
```

Read the active plan's `.md` for the method design. Read the idea's `.md` for the contribution statement and positioning matrix.

---

## Step 1: Exhibit Planning

Before writing a single word, plan every figure and table the paper needs. Claims drive exhibit design — not the other way around.

### 1.1 Map claims to exhibits

For each final claim, determine what visual evidence supports it:

| Claim | Exhibit Type | Content | Priority |
|-------|-------------|---------|----------|
| Main result | table | Main comparison table (method vs baselines) | Must-have |
| Main result | figure | Training curves / convergence | Should-have |
| Ablation finding | table | Ablation results | Must-have |
| Analysis insight | figure | Visualization of the mechanism | Must-have |
| Robustness | table | Results across conditions | Should-have |

### 1.2 Plan supplementary exhibits

Supplementary material for Nature-level submissions typically needs:
- Extended result tables (full metrics, all datasets, all seeds)
- Additional ablations not shown in main text
- Implementation details as figures (system architecture, data pipeline)
- Failure case examples
- Hyperparameter sensitivity analysis

### 1.3 Register all exhibits

For each planned exhibit:

```
research_exhibit(action="create",
  title="Main comparison table",
  kind="table",
  label="tab:main_results")

research_exhibit(action="bind_sources",
  id="exh_001",
  experiments=["exp_007", "exp_008", "exp_009"],
  claims=["claim_001"],
  script="scripts/gen_main_table.py",
  data_path="results/")
```

---

## Step 2: Exhibit Production

**Load `references/exhibit-guide.md` for detailed figure/table standards.**

For each registered exhibit:

### 2.1 Generate

Use `master` subagent to draft generation scripts, then use the `write` tool to save each script to `paper/scripts/` or `.research/scripts/`:

```
task(subagent_type="master"):
  "Write a Python script to generate [exhibit description].
   Input data: [experiment IDs and metric paths]
   Output: [figure format and path]
   Style requirements: [from exhibit-guide reference]
   Write scripts to code/ or paper/scripts/. Return a summary of what was created."
```

Run the script, verify the output visually.

### 2.2 Record render

```
research_exhibit(action="render", id="exh_001", output_path="paper/figures/main_table.pdf")
```

### 2.3 Verify

Check data-exhibit consistency:
- Do the numbers in the table match the experiment records?
- Does the figure accurately represent the underlying data?
- Are axes labeled, units specified, legends present?

```
research_exhibit(action="verify", id="exh_001")
```

### 2.4 Review visual quality

Use `editor` subagent for visual quality assessment:

```
task(subagent_type="editor"):
  "Review this figure/table for publication quality:
   [describe the exhibit]
   Check: resolution, font size, color scheme, accessibility (colorblind-safe),
   caption completeness, axis labels, legend, grid lines, overall clarity.
   Return your editorial feedback as structured text with specific location references."
```

Record the review:
```
research_exhibit(action="review", id="exh_001",
  reviewer="editor", focus="visual quality", ...)
```

### 2.5 Approve

Once verified and reviewed:
```
research_exhibit(action="approve", id="exh_001")
```

---

## Step 3: Paper Structure

### 3.1 Register paper

```
research_paper(action="create",
  title="[paper title]",
  venue="[target venue]",
  source_dir="paper",
  sections=[
    {name: "abstract"},
    {name: "introduction"},
    {name: "related_work"},
    {name: "method"},
    {name: "experiments"},
    {name: "results"},
    {name: "discussion"},
    {name: "conclusion"},
    {name: "supplementary"}
  ])
```

### 3.2 Bind claims and exhibits

```
research_paper(action="bind",
  id="paper_001",
  claims=["claim_001", "claim_002", "claim_003"],
  exhibits=["exh_001", "exh_002", "exh_003", "exh_004"])
```

### 3.3 Advance to drafting

```
research_paper(action="advance", id="paper_001", target_status="drafting")
```

---

## Step 3.5: Pre-Writing Gates(硬门禁)

> **在任何正文写作开始之前,必须依次通过以下三个门禁。** 门禁失败 = 不得动笔。
> 断点续跑:进入本阶段时先运行
> `.research/scripts/compose_progress_check.py .research/compose/ --json`,
> 从第一个未通过的 gate 继续,不要重做已通过的阶段。

### 3.5.1 Contribution Gate(贡献契约)

**Load `references/contribution.md`。**

写作前必须存在 `.research/compose/confirmed_contribution.md`,包含四个 section:
`Core Contribution` / `Why This Contribution Is Needed` / `How This Paper Responds` / `Claim Boundary`。
每个字段与对象模型对齐:
- `Main contribution statement` ↔ `research_claim` 的 statement
- `Evidence available` ↔ `research_experiment` metrics + `research_exhibit`
- `Claim boundary` ↔ claim 的 caveats

门禁:
```
python .research/scripts/contribution_check.py .research/compose/ --json
```
exit 0 才可继续。占位符(TODO/TBD/...)视为失败。

### 3.5.2 Results Validation Gate(结果验证矩阵)

**Load `references/results-validation.md`。**

写入 `.research/compose/results_validation.md`,七列表格:
`Results Unit | Contribution Claim Tested | Result/Evidence | Figure/Table | Confirmatory Condition | Allowed Interpretation | Interpretation NOT Allowed`。
每个主要 Results subsection 必须验证至少一个贡献承诺;`Contribution Claim Tested` 或 `Result/Evidence` 为空 = 硬失败。

门禁:
```
python .research/scripts/results_validation_check.py .research/compose/ --json
```

### 3.5.3 Reviewer Audit Gate(审稿人预演)

**Load `references/reviewer-audit.md`。**

写入 `.research/compose/reviewer_audit.md`(Reviewer Value Map + Objection Register + Editorial Fit)。
用三个视角预演异议:
- reviewer-method:机制与设计选择
- reviewer-evidence:数字、基线、可复现性
- reviewer-writing:贡献清晰度与过度声称

每条 objection 必须有 planned defense;找不到诚实响应的 objection = 真实弱点,
要么软化贡献(改 confirmed_contribution.md 重跑 3.5.1),要么补证据(回 experiment 阶段)。

### 3.5.4 Blueprints + Scene Routing

1. **场景路由**:目标 venue 是期刊 → 读 `references/scenario-journal.md`;会议 → `references/scenario-conference.md`;
   报告/综述 → writing-guide 通用策略。场景 playbook 优先于通用 writing-guide。
2. **Section Blueprints**:逐 section 写出蓝图(段落级):每段的功能、支撑的贡献、证据锚点。
3. **Writing Rationale Matrix**:表格形式,每行一个写作单元,列含:
   `Row ID | Manuscript Unit | Function | Contribution Link | SOTA/Example Pattern | Venue Norm | Evidence Anchor | Planned Change | Final Text Check`。
   第一行论证整体框架;少于 8 行或内容泛化 = 不合格。

### 3.5.5 门禁通过后

- 更新 `research_paper(action="update", id="paper_XXX", ...)` 记录门禁状态
- 在 journal 记录门禁结果:`research_journal(action="append", kind="decision_rationale", note="Pre-writing gates passed: contribution/results-validation/reviewer-audit")`
- 之后才进入 Step 4 正文写作

## Step 4: Section-by-Section Writing

**Load `references/writing-guide.md`(场景路由 + 贡献契约)和对应场景 playbook(scenario-journal / scenario-conference)。**
**Load `references/prose-quality.md` for paragraph-level quality control.**
**前置条件:Step 3.5 三个门禁(contribution / results-validation / reviewer-audit)已全部通过。**

### Writing approach

- Use `scribe` subagent for initial drafts of each section (scribe returns text; main agent writes to `paper/` using the `write` tool)
- Provide scribe with: claim objects, exhibit descriptions, positioning matrix, method design, **broader insights from compose (claim-build)**
- After each section draft, review with `editor` subagent (editor returns text; main agent records via `research_paper(action="review")`)

### Section order (recommended)

Write in this order for maximum coherence:

1. **Method** — write first because it anchors the technical content
2. **Results** — write second because it directly renders the claims
3. **Introduction** — write third because now you know what the paper delivers
4. **Related Work** — load `lit-knowledge` skill for wiki data
5. **Discussion** — write honestly about limitations, broader perspective, and future work
6. **Abstract** — write last because it must accurately summarize everything
7. **Conclusion** — brief, forward-looking, no overclaim

### For each section:

```
task(subagent_type="scribe"):
  "Draft the [section name] section for a paper titled '[title]' targeting [venue].

   Key content:
   [section-specific material — claims, method design, experiment results, etc.]

   Constraints:
   [from writing-guide reference — tone, length, structure requirements]
   Return the drafted text. The main agent writes it to the correct location."
```

### Special instructions for Introduction:

When drafting the Introduction, provide scribe with the broader significance statement from the contribution positioning (novelty-ground) AND the broader insights from the compose (claim-build) phase. Include this directive:

> "After the contribution list and main result preview, include a paragraph that connects this work to the bigger picture. Draw on the broader significance and insights provided. Ask: if this method works, what changes in how people approach [broader problem domain]? Be specific — name the community, the assumption being challenged, the capability being unlocked. Do NOT use generic phrases like 'opens new research directions' — state the specific direction."

### Special instructions for Discussion:

When drafting the Discussion, include this directive:

> "Include a 'Broader Perspective' paragraph before limitations. This paragraph positions the work within the trajectory of the field. Use the validated insights from the compose (claim-build) phase. What does this result suggest about [the fundamental question underlying the research]? What established assumption does it challenge or confirm? What new questions does it open — not implementation tasks, but genuine research questions? Write this for a reader who works in an adjacent area and wants to know why your finding matters to them."

After draft:
```
task(subagent_type="editor"):
  "Review this draft [section name] section:
   [paste draft]

   Check: clarity, logic flow, claim-evidence alignment, honest scope,
    venue-appropriate tone and length.
    Return your editorial feedback as structured text with specific location references."

Record paper-level review:
```
research_paper(action="review", id="paper_001",
  reviewer="editor", focus="[section] narrative quality", ...)
```

---

## Step 5: Prose Quality Audit + Narrative Coherence

**Load `references/prose-quality.md` for the full 5-pass audit protocol.**

### 5.1 Run the 5-pass prose quality audit

For each section, apply (in order):
1. **Clutter extraction** — remove filler phrases, redundancies, AI-isms
2. **Active voice check** — convert passive, resurrect smothered verbs
3. **Sentence architecture** — split >40 word sentences, fix subject-verb distance
4. **Keyword consistency** — verify technical terms are used identically everywhere (the Banana Rule)
5. **Numerical integrity** — verify numbers in text match tables/figures

Use `editor` agent for passes 1-4, `auditor` agent for pass 5.

### 5.2 Reverse outline test

Extract the first sentence of every paragraph and read them in sequence. They should form a coherent story: problem → gap → method → evidence → conclusion. Fix any paragraphs whose topic sentence doesn't advance the narrative.

### 5.3 Full-paper coherence check

After all sections are drafted and quality-audited, do a full-paper coherence check:

```
task(subagent_type="editor"):
  "Review the complete paper draft for narrative coherence:

   [paste full paper or provide file path]

   Check:
   1. Does the introduction promise exactly what the paper delivers?
   2. Does each claim in the results trace to the method section?
   3. Is related work fair and complete?
   4. Are limitations honest?
   5. Is the abstract accurate?
   6. Are cross-references consistent (figures, tables, equations, sections)?
   7. Is the writing style consistent throughout?
   Return your editorial feedback as structured text with specific location references."
```

Record:
```
research_paper(action="review", id="paper_001",
  reviewer="editor", focus="full paper coherence", ...)
```

---

## Step 6: Supplementary Material

### 6.1 Structure

Create the supplementary with:
- Extended result tables (all metrics, all datasets, all seeds)
- Additional ablation experiments
- Detailed hyperparameter tables
- Architecture diagrams
- Algorithm pseudocode (if not in main text)
- Additional visualizations
- Failure case examples
- Dataset details and preprocessing steps

### 6.2 Register supplementary exhibits

Each supplementary item should be a registered exhibit:
```
research_exhibit(action="create",
  kind="supplementary_table",
  title="Full ablation results across all datasets",
  ...)
```

### 6.3 Cross-reference

Ensure main text references supplementary where appropriate:
- "See Appendix A for full results across all datasets"
- "Supplementary Figure S1 shows the training curves"

---

## Step 7: LaTeX / Formatting

**Load `references/latex-conventions.md` for venue-specific formatting.**

**Citation guard(硬门禁)** — 运行:
```
python .research/scripts/latex_guard.py paper/ --json
```
检查:无字面方括号数字(`[1]`)、main tex 含 `\title` + `\maketitle`、`\cite` 键全部有定义。
任一 FAIL → 修复后重跑,不得跳过。

**Numeric consistency(advisory)** — 运行:
```
python .research/scripts/numeric_consistency_check.py paper/ --json
```
报告实验 metrics 与正文数字的缺失/相近不等候选,人工核查后修复。

- Compile the paper to verify no LaTeX errors
- Check page count against venue limits
- Verify figure/table placement
- Check bibliography formatting
- Verify supplementary compiles separately if required

```
bash: cd paper && latexmk -pdf main.tex 2>&1 | tail -20
```

---

### Claim Gate (v3.0 — 前置)

> v2.1 的 Claim Gate 已前置到 Step 3.5 的 Pre-Writing Gates。
> 此处为 paper-ready 前的复核:claims 分类(strong/moderate/weak)、映射到 sections、
> evidence links、caveats、用户确认 paper ambition——这些决策在 Step 3.5.1 已做,
> 这里只验证写作没有漂移。

1. Re-verify each claim classification against `confirmed_contribution.md` Claim Boundary
2. Re-map claims to paper sections(writing 可能引入新声称)
3. Re-verify every claim has evidence links(未在 Step 3.5.2 矩阵中的 subsection 是红旗)
4. Re-check caveats for moderate/weak claims
5. User confirms paper ambition
---

## Step 8: Advance to Ready

### 8.1 Submission Readiness Checkpoint

**⏸ CHECKPOINT — do not proceed without user input.**

**Checkpoint kind: `submission_readiness`**

Before marking the paper ready or advancing to submission, the user must confirm submission readiness.

Present:
- Section completion status
- Exhibit approval status
- Claim-exhibit binding coverage
- Page count and venue limit
- Claim strength classification (strong/moderate/weak per claim)
- RQG-backed evidence summary
- Known remaining issues
- Overclaim check status

**Options for user**:
1. **Mark ready for submission** — paper is complete and accurate
2. **Request revisions** — specific sections or claims need work
3. **Add more experiments** — evidence gap identified during writing
4. **Reduce claim strength** — downgrade strong claims to moderate/weak
5. **Park for later** — not ready to submit now

Record user decision rationale in journal.
**In autonomous mode**: Agent may waive but MUST log waiver with reasoning to `phase_run.human_checkpoints`.

When a human checkpoint is triggered, the system auto-generates a checkpoint brief. The brief provides full context for the human decision. If you need to regenerate or view it, use `research_checkpoint_brief(action="generate"|"read")`.

### 8.2 Mark paper ready

Once the user confirms:

```
research_paper(action="advance", id="paper_001", target_status="ready")
```

### 8.3 Record context refresh

```
research_state(action="brief",
  context_refresh={
    used_wiki_refs: [...],
    phase_run: "paper-compose",
    key_decisions: ["paper marked ready", "submission_readiness confirmed"],
    checked_skill_rules: [
      "Every paper-visible result references specific experiment/exhibit",
      "Claims map to RQG-backed evidence",
      "StorySpine drives intro arc",
      "Overclaim check passes before paper-ready"
    ],
    drift_check: { status: "pass|warning|block", note: "..." }
  })
```

### 8.4 Advance state

```
research_state(action="advance", target_phase="compose",  → then invoke paper-audit skill
  summary="Paper draft complete, ready for pre-submission audit",
  reason="All sections drafted, exhibits approved, claims bound, submission readiness confirmed",
  refs_paper="paper_001",
  refs_claims=[...],
  refs_exhibits=[...])
```

---

## Participation Mode Behavior

- **collaborative**: Pause after exhibit planning (Step 1), after each section draft, and at final checkpoint
- **guided**: Pause at final checkpoint only
- **autonomous**: Full workflow, pause only if exhibits fail verification

---

## Error Recovery

| Situation | Action |
|-----------|--------|
| Writing exposes unsupported claim | Rollback to compose (claim-build): `research_state(action="redirect", target_phase="compose", reason="Writing revealed unsupported claim: ...")` → then invoke claim-build skill |
| Gate fails (contribution/results-validation/reviewer-audit) | Fix the artifact and re-run its check script; do NOT proceed to writing. If evidence is missing, rollback to `experiment`. |
| Exhibit data inconsistent | Re-verify with auditor. If experiment data is wrong, rollback to `experiment`. |
| Page limit exceeded | Prioritize: move content to supplementary, compress tables, tighten prose. Do NOT remove claims — move supporting evidence to appendix. |
| Section draft quality too low | Re-run scribe with more context. Use editor for specific feedback. Iterate. |
| Missing baseline comparison | Rollback to `experiment` to add the missing baseline. |
| User/editor gives feedback on existing draft | Route to `paper-revise` skill for targeted section-level revision: `skill(name="paper-revise")` |

---

## Rollback Entry Points

### From audit (narrative/format issues)
Re-enter at Step 4 (section writing) or Step 7 (formatting) depending on the issue.

### From venue-cycle (reviewer demands rewrite)
Re-enter at Step 4. The reviewer comments guide which sections need revision.
